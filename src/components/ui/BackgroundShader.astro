<canvas
  class="background-shader"
  aria-label="Animated background"
></canvas>

<style>
  .background-shader {
    position: fixed;
    /* top: 0; */
    inset: 0;
    width: 100%;
    height: 100vh;
    height: 100dvh;
    z-index: 0;
    pointer-events: none;
    /* Promote to own compositing layer — prevents flicker on mobile scroll */
    will-change: transform;
  }
</style>
