---
import IndexLayout from '@/layouts/IndexLayout.astro'
import Button from '@/components/ui/Button.astro'
import BackgroundShader from '@/components/ui/BackgroundShader.astro'
---

<IndexLayout
  title="Way2 — The Network for Crowd-Scale Events"
  description="Reliable communication and coordination for live events. Way2 provides a resilient mesh network for live event organizers and attendees — no cell service required."
>
  <BackgroundShader />

  <main class="editorial-grid">

    <!-- ════════════════════════════════════════════ -->
    <!-- HERO                                        -->
    <!-- ════════════════════════════════════════════ -->
    <section class="hero-section grid-full" aria-labelledby="hero-heading">
      <div class="hero-frame">
        <picture class="hero-bg-image">
          <source type="image/webp" srcset="/media/embrace.webp" />
          <img
            src="/media/embrace.png"
            alt="Two people embracing in a crowd at a live event"
            width="1440"
            height="810"
            fetchpriority="high"
          />
        </picture>

        <div class="hero-content">
          <h1 id="hero-heading" class="heading-display hero-headline">
            The Crowd-Scale Network for Live Events
          </h1>
          <div class="hero-footer">
            <p class="hero-subhead">
              Reliable communication and coordination for live events.<br />
              <span class="text-highlight">No compass or cell service required.</span>
            </p>
            <div class="hero-actions">
              <Button href="#organizers" label="For Organizers" variant="primary" />
              <Button href="#attendees" label="For Attendees" variant="secondary" />
            </div>
          </div>
        </div>
      </div>
    </section>

    <section class="network-break grid-full scroll-reveal" aria-label="The problem Way2 solves">
      <div class="network-break-inner">
        <h3 class="heading-display network-break-heading">
          When the network dies,<br />everyone feels it
        </h3>
        <div class="network-break-body">
          <p>
            Network failure means frustrated fans, disrupted operations, and lost revenue.
            In the worst case, lives are at risk and PR nightmares erupt.
          </p>
          <p class="network-break-kicker">People don't return.</p>
        </div>
      </div>
    </section>

    <!-- ════════════════════════════════════════════ -->
    <!-- (01) ORGANIZERS                             -->
    <!-- ════════════════════════════════════════════ -->
    <span class="section-label label-mono scroll-reveal" id="organizers">(01) — For Organizers</span>

    <h2 class="heading-display section-heading organizer-heading scroll-reveal">
      Secure Your Event
    </h2>

    <p class="body-editorial section-body scroll-reveal">
      Typical networks fail when they are needed most. Way2 provides a dedicated, resilient mesh network for your event operations and attendee safety.
    </p>

    <div class="demo-device scroll-reveal">
      <video
        autoplay
        loop
        muted
        playsinline
        width="220"
        height="470"
        aria-label="Way2 app walkthrough showing friend-finding and chat features"
      >
        <source src="/media/demo-walkthrough-loop.webm" type="video/webm" />
        <source src="/media/demo-walkthrough.mp4" type="video/mp4" />
      </video>
    </div>

    <div class="feature-grid scroll-reveal" role="list" aria-label="Organizer features">
      <article class="feature-card glass-panel-dark" role="listitem" style="--i:0">
        <span class="feature-icon" aria-hidden="true">✅</span>
        <h3 class="feature-title">Easy Setup</h3>
        <p>We handle setup and logistics so you can focus on delivering an unforgettable event.</p>
      </article>
      <article class="feature-card glass-panel-dark" role="listitem" style="--i:1">
        <span class="feature-icon" aria-hidden="true">🛡️</span>
        <h3 class="feature-title">Safety, Simplified</h3>
        <p>Improve emergency response with precise attendee locations and reliable communications.</p>
      </article>
      <article class="feature-card glass-panel-dark" role="listitem" style="--i:2">
        <span class="feature-icon" aria-hidden="true">📡</span>
        <h3 class="feature-title">Superior Coordination</h3>
        <p>Better communication tools that beat walkie-talkies — simple, reliable, no training required.</p>
      </article>
      <article class="feature-card glass-panel-dark" role="listitem" style="--i:3">
        <span class="feature-icon" aria-hidden="true">💸</span>
        <h3 class="feature-title">App Profitability</h3>
        <p>Turn your event app into a revenue generator instead of an expense.</p>
      </article>
      <article class="feature-card glass-panel-dark" role="listitem" style="--i:4">
        <span class="feature-icon" aria-hidden="true">🗺️</span>
        <h3 class="feature-title">Custom Interactive Maps</h3>
        <p>Intuitive maps and waypoints ensure seamless navigation to premium amenities.</p>
      </article>
      <article class="feature-card glass-panel-dark" role="listitem" style="--i:5">
        <span class="feature-icon" aria-hidden="true">📱</span>
        <h3 class="feature-title">Premium Branded Experience</h3>
        <p>Deliver exclusive, branded digital experiences for your community.</p>
      </article>
    </div>

    <div class="organizer-cta scroll-reveal">
      <p class="body-editorial">
        Way2 connects phones directly, creating a living, breathing local network that thrives in dense crowds — <strong class="text-highlight">the bigger the crowd, the better it works.</strong>
      </p>
      <Button
        href="https://docs.google.com/forms/d/e/1FAIpQLSduCM6cOmQd2xNFpeqKcyyYNI8efWHrgY4u1Rdln4XoYUSQsw/viewform?usp=header"
        label="Book a Demo"
        variant="primary"
        target="_blank"
        rel="noopener noreferrer"
      />
    </div>

    <!-- ════════════════════════════════════════════ -->
    <!-- (02) ATTENDEES                              -->
    <!-- ════════════════════════════════════════════ -->
    <span class="section-label label-mono scroll-reveal" id="attendees">(02) — For Attendees</span>

    <h2 class="heading-display section-heading attendee-heading scroll-reveal">
      Lose Yourself,<br />Not Your Friends
    </h2>

    <div class="phone-cluster scroll-reveal">
      <img
        class="phone-mockup phone-chat"
        src="/media/tg-proto-chat.webp"
        alt="Way2 chat interface showing group messaging in a crowd"
        width="280"
        height="600"
        loading="lazy"
        decoding="async"
      />
      <img
        class="phone-mockup phone-ar"
        src="/media/tg-proto-ar.webp"
        alt="Way2 AR view showing friend locations overlaid on camera"
        width="280"
        height="600"
        loading="lazy"
        decoding="async"
      />
    </div>

    <div class="attendee-features scroll-reveal">
      <ul class="feature-list" role="list">
        <li><span class="feature-icon" aria-hidden="true">✈️</span> <strong>Works in Airplane Mode</strong> — No connectivity? No problem.</li>
        <li><span class="feature-icon" aria-hidden="true">📍</span> <strong>Friend Finding</strong> — Precise directions to your crew, anywhere in the crowd.</li>
        <li><span class="feature-icon" aria-hidden="true">🗺️</span> <strong>Event Maps</strong> — Find stages, vendors, toilets... your tent!</li>
        <li><span class="feature-icon" aria-hidden="true">🛟</span> <strong>Safety Check-ins</strong> — Get help or notify your group instantly.</li>
        <li><span class="feature-icon" aria-hidden="true">🔐</span> <strong>Private</strong> — Only the friends you add can see you.</li>
      </ul>
      <Button
        href="https://docs.google.com/forms/d/e/1FAIpQLSduCM6cOmQd2xNFpeqKcyyYNI8efWHrgY4u1Rdln4XoYUSQsw/viewform?usp=header"
        label="Get the App"
        variant="secondary"
        target="_blank"
        rel="noopener noreferrer"
      />
    </div>

    <blockquote class="pull-quote scroll-reveal">
      <p>&ldquo;The bigger the crowd, the better it works.&rdquo;</p>
    </blockquote>

    <!-- ════════════════════════════════════════════ -->
    <!-- (03) TESTIMONIALS                           -->
    <!-- ════════════════════════════════════════════ -->
    <span class="section-label label-mono grid-full scroll-reveal" style="text-align:center;">(03) — What People Say</span>

    <div class="testimonial-hero grid-full scroll-reveal">
      <blockquote>
        <p>&ldquo;Safety is our biggest concern. Way2 is a game changer.&rdquo;</p>
        <cite>— Event Organizer</cite>
      </blockquote>
    </div>

    <div class="testimonial-pair scroll-reveal">
      <blockquote class="testimonial-small">
        <p>&ldquo;Dude, I needed this at the gorge last weekend.&rdquo;</p>
        <cite>— Festival Attendee</cite>
      </blockquote>
      <blockquote class="testimonial-small">
        <p>&ldquo;Finally, reliable comms without the cell tower overload.&rdquo;</p>
        <cite>— Production Manager</cite>
      </blockquote>
    </div>

  </main>
</IndexLayout>

<!-- Scroll reveal observer -->
<script>
  function initScrollReveal() {
    const els = document.querySelectorAll('.scroll-reveal');
    if (!els.length) return;

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add('in-view');
            observer.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.1, rootMargin: '0px 0px -40px 0px' }
    );

    els.forEach((el) => observer.observe(el));
  }

  initScrollReveal();
  document.addEventListener('astro:page-load', initScrollReveal);
</script>

<style>
  /* ─── Scroll Reveal ─── */
  .scroll-reveal {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.8s ease-out, transform 0.8s ease-out;
  }

  /* Staggered delays for cinematic entrance */
  .section-label.scroll-reveal { transition-delay: 0ms; }
  .section-heading.scroll-reveal { transition-delay: 100ms; }
  .section-body.scroll-reveal { transition-delay: 200ms; }
  .feature-grid.scroll-reveal { transition-delay: 300ms; }

  .scroll-reveal.in-view {
    opacity: 1;
    transform: translateY(0);
  }

  @media (prefers-reduced-motion: reduce) {
    .scroll-reveal {
      opacity: 1;
      transform: none;
      transition: none;
    }
  }

  /* ─── Section Labels ─── */
  .section-label {
    grid-column: 1 / -1;
    padding-top: 6rem;
    scroll-margin-top: 5rem;
  }

  @media (min-width: 768px) {
    .section-label {
      grid-column: 2 / 5;
      padding-top: 8rem;
    }
  }

  /* ─── Section Headings ─── */
  .section-heading {
    grid-column: 1 / -1;
    margin: 0;
  }

  @media (min-width: 768px) {
    .organizer-heading {
      grid-column: 2 / 9;
    }
    .attendee-heading {
      grid-column: 5 / 13;
      text-align: right;
    }
  }

  /* ─── Section Body ─── */
  .section-body {
    grid-column: 1 / -1;
    margin: 0;
  }

  @media (min-width: 768px) {
    .section-body {
      grid-column: 2 / 7;
    }
  }

  /* ─── HERO ─── */
  .hero-section {
    overflow: hidden;
    padding: 0.5rem;
  }

  @media (min-width: 768px) {
    .hero-section {
      padding: 1rem;
    }
  }

  .hero-frame {
    position: relative;
    width: 100%;
    min-height: calc(100dvh - 2rem);
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    overflow: hidden;
    border-radius: 1.6rem;
    overflow: hidden;
    border: 1px solid var(--colors-ink-60);
  }

  .hero-bg-image {
    position: absolute;
    inset: 0;
    z-index: 0;
    background-color: var(--colors-teal-10);
    mix-blend-mode: difference;
  }

  .hero-bg-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center top;
  }

  .hero-content {
    position: relative;
    z-index: 1;
    padding: 2rem 1.25rem 3rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .hero-headline {
    margin: 0;
    max-width: 18ch;
  }

  .hero-footer {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .hero-subhead {
    font-size: var(--font-size-xl);
  }

  .text-highlight {
    color: var(--colors-teal-95);
    font-weight: 400;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  @media (min-width: 768px) {
    .hero-content {
      padding: 4rem;
    }
  }

  @media (min-width: 2000px) {
    .hero-content {
      padding: 4rem;
    }

    .hero-frame {
      min-height: calc(100dvh - 12rem);
    }
  }

  @media (max-width: 420px) {
    .hero-content {
      padding: 1.5rem 1.25rem 2.5rem;
    }

    .hero-subhead {
      font-size: 1rem;
    }
  }

  /* ─── DEMO DEVICE ─── */
  .demo-device {
    grid-column: 1 / -1;
    display: flex;
    justify-content: center;
    padding: 2rem 0;
  }

  .demo-device video {
    width: 200px;
    height: auto;
    border-radius: 33px;
    border: 6px solid #1a1a1a;
    box-shadow: 0 40px 80px rgba(0, 0, 0, 0.5);
    transform: rotate(2deg);
    background: #000;
  }

  @media (min-width: 768px) {
    .demo-device {
      grid-column: 9 / 13;
      grid-row: span 2;
      align-items: start;
      padding: 0;
      margin-top: -2rem;
    }

    .demo-device video {
      width: 240px;
    }
  }

  /* ─── FEATURE GRID ─── */
  .feature-grid {
    grid-column: 1 / -1;
    display: grid;
    grid-template-columns: 1fr;
    gap: 1rem;
    padding: 2rem 0;
  }

  @media (min-width: 480px) {
    .feature-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 768px) {
    .feature-grid {
      grid-column: 2 / 12;
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .feature-card {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    padding: 1.5rem;
    border-radius: 12px;
    transition: transform 0.3s ease, opacity 0.8s ease-out;
    transition-delay: calc(var(--i, 0) * 80ms);
  }

  .feature-card:hover {
    transform: translateY(-2px);
  }

  .feature-icon {
    font-size: 1.75rem;
    line-height: 1;
  }

  .feature-title {
    font-family: var(--font-heading);
    font-size: 1.1rem;
    font-weight: 600;
    margin: 0;
    color: var(--colors-teal-95);
  }

  .feature-card p {
    margin: 0;
    font-size: 0.9rem;
    line-height: 1.5;
    color: rgba(255, 255, 255, 0.7);
  }

  /* ─── NETWORK BREAK ─── */
  .network-break {
    padding: 0 1rem 1rem;
  }

  .network-break-inner {
    background:
      url('/media/network-dies-bg.webp') no-repeat center center / cover,
      var(--colors-red-20);
    background-blend-mode: exclusion, darken;
    margin: 0 auto;
    padding: 4rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    border-radius: 1.6rem;
    border: 1px solid var(--colors-indigo-10);
    color: var(--colors-indigo-30);
  }

  .network-break-heading {
    font-size: clamp(2rem, 5vw, 4rem);
    margin: 0;
  }

  .network-break-body {
    max-width: 50ch;
    color: var(--colors-indigo-100);
    font-size: var(--font-size-l);
  }

  .network-break-kicker {
    font-weight: 700;
  }

  /* ─── ORGANIZER CTA ─── */
  .organizer-cta {
    grid-column: 1 / -1;
    display: flex;
    flex-direction: column;
    gap: 2rem;
    padding: 3rem 0 2rem;
    border-bottom: 1px solid var(--border);
  }

  @media (min-width: 768px) {
    .organizer-cta {
      grid-column: 2 / 8;
      padding: 4rem 0 6rem;
    }
  }

  /* ─── PHONE CLUSTER ─── */
  .phone-cluster {
    grid-column: 1 / -1;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 400px;
    padding: 2rem 0;
  }

  @media (min-width: 768px) {
    .phone-cluster {
      grid-column: 2 / 7;
      min-height: 550px;
    }
  }

  .phone-mockup {
    position: absolute;
    width: 200px;
    height: auto;
    border-radius: 32px;
    border: 4px solid #1a1a1a;
    background-size: 200% 200%;
  }

  @media (min-width: 768px) {
    .phone-mockup {
      width: 240px;
      border-width: 8px;
      border-radius: 40px;
    }
  }

  .phone-chat {
    transform: rotate(-8deg) translateX(-60px) translateY(-15px);
    z-index: 1;
  }

  .phone-ar {
    transform: rotate(8deg) translateX(60px) translateY(15px);
    z-index: 2;
  }

  @media (max-width: 420px) {
    .phone-mockup {
      width: 160px;
    }
    .phone-chat {
      transform: rotate(-6deg) translateX(-45px) translateY(-10px);
    }
    .phone-ar {
      transform: rotate(6deg) translateX(45px) translateY(10px);
    }
  }

  /* ─── ATTENDEE FEATURES ─── */
  .attendee-features {
    grid-column: 1 / -1;
    display: flex;
    flex-direction: column;
    gap: 2rem;
    padding: 1rem 0 2rem;
  }

  @media (min-width: 768px) {
    .attendee-features {
      grid-column: 7 / 12;
      padding: 2rem 0;
    }
  }

  .feature-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .feature-list li {
    display: flex;
    align-items: baseline;
    gap: 0.75rem;
    font-size: 1rem;
    line-height: 1.5;
    color: rgba(255, 255, 255, 0.85);
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    padding: 1rem 0;
  }

  .feature-list li .feature-icon {
    font-size: 1.25rem;
    flex-shrink: 0;
  }

  .feature-list strong {
    color: var(--colors-ink-100);
  }

  /* ─── PULL QUOTE ─── */
  .pull-quote {
    grid-column: 1 / -1;
    margin: 0;
    padding: 4rem 0 6rem;
    border-bottom: 1px solid var(--border);
    text-align: center;
  }

  .pull-quote p {
    font-family: var(--font-heading);
    font-style: italic;
    font-size: clamp(1.75rem, 3vw + 0.5rem, 3.5rem);
    line-height: 1.15;
    color: var(--colors-teal-95);
    margin: 0;
    max-width: 20ch;
    margin-inline: auto;
  }

  @media (min-width: 768px) {
    .pull-quote {
      grid-column: 3 / 11;
    }
  }

  /* ─── TESTIMONIALS ─── */
  .testimonial-hero {
    text-align: center;
    padding: 2rem 0 4rem;
  }

  .testimonial-hero blockquote {
    margin: 0;
  }

  .testimonial-hero p {
    font-family: var(--font-heading);
    font-style: italic;
    font-size: clamp(1.5rem, 3vw + 0.5rem, 3rem);
    line-height: 1.15;
    color: var(--colors-ink-100);
    margin: 0 auto 1.5rem;
    max-width: 22ch;
  }

  .testimonial-hero cite,
  .testimonial-small cite {
    font-family: var(--font-mono);
    font-size: 0.85rem;
    color: var(--colors-teal-95);
    font-style: normal;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .testimonial-pair {
    grid-column: 1 / -1;
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    padding-bottom: 6rem;
  }

  @media (min-width: 768px) {
    .testimonial-pair {
      grid-column: 2 / 12;
      grid-template-columns: 1fr 1fr;
      gap: 4rem;
    }
  }

  .testimonial-small {
    margin: 0;
    border-left: 2px solid var(--colors-teal-95);
    padding-left: 1.5rem;
  }

  .testimonial-small p {
    font-size: 1.25rem;
    line-height: 1.4;
    margin: 0 0 0.75rem;
    color: rgba(255, 255, 255, 0.9);
  }
</style>
